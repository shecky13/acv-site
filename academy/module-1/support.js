/**
 * ART-CREAVISION® ACADEMY — Module 1 : Fondations de l'IA générative
 * support.js
 * ------------------------------------------------------------------
 * ÉTAT ACTUEL : fichier réservé (stub), volontairement non fabriqué.
 *
 * Constat technique :
 * Le fichier source de ce module (maquette "Module 1 — Fondations de
 * l'IA générative") est une page 100% statique : HTML + CSS uniquement.
 * Aucune logique JavaScript propre au module n'existe dans la source
 * d'origine (pas de quiz interactif scripté, pas d'état, pas de
 * sauvegarde de progression, pas d'appel réseau).
 *
 * Ce fichier est donc chargé par index.html (<script src="support.js"
 * defer>) pour respecter l'arborescence demandée et pour être prêt à
 * accueillir de l'interactivité future, mais il ne contient
 * aujourd'hui aucune fonctionnalité inventée : la charte du projet
 * interdit de modifier le design, les textes ou les interactions du
 * Module 1 sans nécessité technique, donc rien n'a été ajouté qui ne
 * soit pas déjà dans la page.
 *
 * Ce que ce fichier fait aujourd'hui :
 *  - Rien de visible pour l'utilisateur. Il se charge sans erreur et
 *    ne modifie aucun élément du DOM.
 *  - Il consigne une seule ligne dans la console (mode développeur
 *    uniquement) pour confirmer que le chargement a réussi — utile
 *    pour la vérification technique demandée (point 9).
 *
 * Prochaine étape possible (hors périmètre de cette livraison) :
 *  - Si une interactivité est un jour demandée pour ce module
 *    (quiz scripté, sauvegarde de progression locale, connexion à
 *    l'écosystème ACADEMY existant dans /academy/), le code
 *    correspondant viendra ici, dans une nouvelle demande explicite.
 */

(function () {
  "use strict";

  try {
    console.log(
      "[ART-CREAVISION ACADEMY] Module 1 — support.js chargé avec succès (aucune interactivité définie pour le moment)."
    );
  } catch (e) {
    /* silencieux : ne jamais bloquer l'affichage de la page pour un log */
  }
})();
